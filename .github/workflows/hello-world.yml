# name
name: hello-world

# triggers
on:
  - push

# one or more jobs
jobs:
  greetings:
    runs-on: ubuntu-latest
    steps:
      - id: Say-Hello
        run: echo "Hello, world!"
    #   - id: Second-Step
    #     run: ls
      - id: Cow-Says
        uses: mscoutermarsh/cowsays-action@master
        with:
            text: 'ship ship ship!'
            color: 'red'